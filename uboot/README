Yes, there's a binary file in here.

It's from a really old version (Feb 9, 2020) of Megi's 5.6 uboot build.

The newer builds no longer contain u-boot :(

This will be removed when the issue is fixed.
